<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="roundedCornerImageStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">25dp</item>
    </style>

    <style name="operationDialogFragment" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@android:color/transparent</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:background">@android:color/transparent</item>
        <!-- 背景色 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:windowFullscreen">true</item>
        <!-- 全屏 -->
    </style>
    <style name="TabLayoutTextStyle">
        <item name="android:textSize">@dimen/tl_textSize</item>
    </style>

    <!-- 选择框 -->
    <style name="CheckBoxTheme">
        <item name="colorAccent">@color/selectState</item><!--选中颜色-->
        <item name="colorControlNormal">@color/defaultTextColor</item><!--未选中颜色-->
    </style>
</resources>